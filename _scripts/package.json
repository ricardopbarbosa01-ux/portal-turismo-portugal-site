{
  "name": "pth-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only utility scripts for PTH maintenance",
  "scripts": {
    "populate-images": "node populate-images.js",
    "populate-images:apply": "node populate-images.js --apply"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.33.5"
  }
}
