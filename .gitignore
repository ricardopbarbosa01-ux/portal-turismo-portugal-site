node_modules/
.wrangler/
supabase/.temp/
.zip.zip
_data/
test-results/
smoke-results.json
smoke-test.png
*.pdf
*.bak

# Playwright
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/tests/visual/screenshots/*.png
/tests/visual/screenshots/*.json
.claude/
.mcp.json

# Local environment — credentials never committed
.env.local
.env*.local

# Test data — contains UUIDs and seed rows, never commit to production
_scripts/test-rls-setup.sql

# Scripts de investigação ad-hoc (não commitar)
/inspect-*.js
/audit-*.log

# Auditorias e scripts ad-hoc
/audit-*.js
/_audit/
/performance-reports/

# Vídeos grandes — servidos via R2 CDN, não commitar
/videos/*.mp4
/videos/*.webm
/videos/*.mov

# GSD (Get Stuff Done) plugin artifacts — Claude Code internal planning
.planning/

# Navbar audit screenshots — local visual diffs, not production
/tools/screenshots-navbar/

# Audit reports — local-only, sensitive findings stay out of repo
/docs/audit-*.md

# README files from imported asset packages (e.g. icon packs)
/README.md
